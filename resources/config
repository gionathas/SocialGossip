#File di configurazione per il server ed il client
SERVER_HOST_NAME=localhost
SERVER_TCP_PORT=5000

SERVER_RMI_SERVICE_NAME = "SocialGossipNotification"
SERVER_RMI_PORT = 6000

#porte per ricezione file nel client
FIRST_PORT = 7000;
LAST_PORT = 8000;